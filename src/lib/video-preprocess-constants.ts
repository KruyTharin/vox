/** Vertical 9:16 HD (Shorts / Reels). */
export const PREPROCESS_TARGET_WIDTH = 1080;
export const PREPROCESS_TARGET_HEIGHT = 1920;

/** Default trim when caller does not override (seconds). */
export const PREPROCESS_DEFAULT_MAX_DURATION_SEC = 22;
